# Prompt log

A running record of prompts in this session, kept per explicit request. Entries
before that request are reconstructed from context — marked where the exact
original wording wasn't preserved. From the point of the request onward, every
prompt is logged verbatim, in order, as it's given — this file gets appended
to on each new turn, not rewritten.

## Session: COMP4020 Assignment 1 — topic exploration (2026-08-10/11)

1. **"clone assignment 1"** — start the Assignment 1 repo via the `comp4020:start` skill.
2. **"have you read teh brief and spec"** [sic] — confirm the brief/spec had been read.
3. *(paraphrased — exact original wording not preserved in context)* Requested a brainstorm for an explainer site on "the current economic state of AI" / "web of AI money" — debt, datacenters, "sneaky loans" — wanting a succinct/clear template, creative interactive ideas, and a feasibility assessment, compiled into a file. Stated priority: "clear and creative communication (leveraging website interactivity)." → produced `notes/topic-brainstorm.md`.
4. **"ok now i want you to do an extensive literature review on the topic - reports on the deals, the money, commentary, speculations, everything, compile this with links and summary for each link and add this to another note"** → produced `notes/literature-review.md`.
5. **"dig deeper - is there an angle here that isn't yet in the limelight, perhaps more of a niche rather than a general summary of the whole sphere - provide an extensive list with sources and summaries and append to the literature review"** → appended Part 2 (sections 12–20) to `notes/literature-review.md`.
6. **"do just a small extra literature review in a seperate file to engage in another idea; the financial movements of donald trump over the past few years (on a corruption / money making angle), also provide some ideas for presenting this in a fun, interactive way."** → produced `notes/trump-finances-literature-review.md`.
7. **"do a comparison on the two topics with pros and cons to help me decide which i should do"** → comparison delivered in chat (AI money web vs. Trump finances).
8. **"im looking for originality and engagement via emotional hook, how can i further these in the ai money topic"** → ideas delivered in chat (personalized slider, 401(k) hook, subprime/GPU-collateral echo, water-rights human voice, REIT surprise beat).
9. **"i want a more emotive element, something maybe to round out the website with an impression"** → closing-beat recommendation delivered in chat (frozen personal receipt + single quote + negative space, no CTA).
10. **"give me more potential quotes, and possible anecdotal accompanyments r tie ins to human impact, also record all of the promts so far and all future prompts in a file as we go"** — this request; logging starts here. → researched and appended "Part 3: quotes and human-impact anecdotes for the page" to `notes/literature-review.md` (electricity bills, noise, water/land, coal/air), and created this log.
11. **"not quite what i was after, more quotes like this one "the danger is these kinds of deals eventually reveal a house of cards" and general statements about the ramifications of this, so flesh out an impacts and ramifications section to teh literature review on the wider population (including the viewer)"** → researched and appended "Part 4: impacts and ramifications — quotes on what happens to the wider population (including the viewer)" to `notes/literature-review.md` (wealth-effect mechanism, "you didn't buy a ticket" framing, too-big-to-fail debate, historical-memory warnings, mark-to-market accounting risk).
12. **"ok now, working in a new note file, i want to refine ideas and work towards a narrative for the website along with an idea for the engaging / interactive accompanyments. I like the stack idea and would like to explore that further amongst others"** → created `notes/narrative-and-mechanic.md`: a five-act narrative (rising tower → lean → loop → "your block" personalization → freeze/quote) folding the stack, loop, and lever templates into one spine, a plain-language statement of the core interaction, a sharpened point-of-view line, and a record of parked directions (network diagram, receipt ticker, standalone human-impact section) with reasons.
13. **"ok lets start with a draft website"** → planned (via plan mode) and built the first-draft prototype: `src/data/commitments.ts` (6-entry sourced dataset), a full rewrite of `src/pages/index.astro` implementing Acts 1/2/4/5 (tower reveal + lean, personalization slider, freeze/quote) with Act 3 simplified to a static loop illustration, extended `src/styles/global.css`, and deletion of `spec/starter.test.ts` now that the starter page is gone. Plan saved to `/Users/brynly/.claude/plans/sorted-coalescing-crab.md`.
14. **"run it on local server"** → started `astro dev` (background daemon, `http://localhost:4324/comp4020-ass1-BrynMtchll/`); confirmed liveness via `curl` after the wrapping Bash task reported "completed" (Astro 7 daemonizes `astro dev`, so task completion doesn't mean the server stopped).
15. **"install and setup agent browser"** → installed the `agent-browser` npm CLI globally (scoped `--allow-scripts` override after reading the postinstall script to confirm it only downloads the package's own official native binary), ran `agent-browser install` for a local Chrome, created a named session (`a1-7b6ee74d25cd`) per the tool's own session-isolation guidance, then used it to verify the draft site end-to-end at both required viewports (1920×1080 and 390×844): screenshots of every section plus `agent-browser eval` checks confirming the scroll-driven lean angle and the slider-driven exposure/lean/block-height math all compute exactly per the design (e.g. lean = 4deg at 3/6 debt blocks revealed, matching `(debtCount/total)*8deg`; slider at -50 → exposure $5,250, lean 8deg, block height 200px, all matching the formulas in `index.astro`'s script). No layout or interaction issues found at either viewport — `pnpm check` remains green.
16. **"what is the proposed linear content flow, list with 1 short sentence per bit"** → answered informationally in chat with a 10-item list walking the page top to bottom (kicker/headline → framing line → tower reveal → lean → loop → slider/exposure → freeze quote → citations); no code changes.
17. **"iterate and work on the site for a while until it looks professional and proper"** → full redesign pass: rewrote `src/styles/global.css` with an editorial/financial-journalism design system (serif display type + sans body, warm paper background, ink/rust-accent/cash/debt palette, card-based sectioning), restructured `index.astro`'s hero/tower/loop/citations markup, and fixed several bugs found via `agent-browser` screenshots at both viewports along the way: a collapsed-whitespace typo in the exposure sentence, a broken full-bleed dark `.freeze` section (wrong `calc()` full-bleed technique), overlapping loop-diagram labels/arrows, and — the significant one — the leaning `.tower` clipping its top blocks off-screen on the 390px phone viewport (a ~1000px-tall flex stack rotating around `transform-origin: bottom center` swings its top far enough sideways to exceed a narrow viewport's margin). Fixed with a runtime-computed safe-lean clamp in `applyLean()` (measures actual rendered block width/tower height and caps the angle so the top never exceeds the viewport), mobile-specific narrower/shorter block sizing so the lean stays visible rather than clamped to near-zero, `.tower { width: fit-content }` so the rotating box matches its visible content instead of the wider container, and `overflow-x: hidden` on `html`/`body` as a safety net. Verified clean at both viewports (no clipping, no horizontal scroll, `pnpm check` green) via repeated `agent-browser` screenshot/eval checks.
